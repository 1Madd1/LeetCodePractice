package org.example.LinkedListSubSumNotZero;

import java.util.LinkedList;

public class LinkedListSubSumNotZero {
    public LinkedList<Integer> linkedListWithoutSubSumZero(LinkedList<Integer> list) {
        int i = 0, j = 1, sum = 1;
//        list.ne
        if (list.size() == 0) {
            return null;
        } else if (list.size() == 1) {
            return list;
        }
        while (i < list.size() && j < list.size()) {
//            sum
        }
        return null;
    }
}
